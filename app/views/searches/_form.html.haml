= form_with(model: search, local: true) do |form|
  - if search.errors.any?
    #error_explanation
      %h2
        = pluralize(search.errors.count, "error")
        prohibited this search from being saved:
      %ul
        - search.errors.full_messages.each do |message|
          %li= message
  .field
    = form.label :status
    = form.number_field :status
  .field
    = form.label :total_results
    = form.number_field :total_results
  .actions
    = form.submit
